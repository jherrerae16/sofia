// La tarea 16 completa este archivo con la lectura real de pesajes y mediciones.
// Por ahora devuelve un mapa vacío para que las tareas que dependen de ella compilen.
export async function pesoVivoPorLote(): Promise<Map<string, number>> {
  return new Map<string, number>()
}
